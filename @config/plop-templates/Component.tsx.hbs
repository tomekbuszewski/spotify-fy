/**
* @author {{ userName }}
* @since {{ created }}
*/

import * as React from "react";

import { Styled{{pascalCase name}} } from "./{{pascalCase name}}.styles";

interface Props {
  children?: React.ReactNode;
}

const {{pascalCase name}} = (props: Props) => (
  <Styled{{pascalCase name}}>{props.children}</Styled{{pascalCase name}}>
);

export { {{pascalCase name}} }
